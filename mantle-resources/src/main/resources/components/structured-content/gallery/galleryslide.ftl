<#-- Component tag is not closed to allow contents of slide to be added as child elements.-->
<#-- A matching EndGroup is added automatically to provide the closing tag.-->
<@component tag="${model.tag}" closeTag=false>
    <@location name="content" tag="" />
</@component>